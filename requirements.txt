# Backend deps (so Render can install from repo root). Includes gradio-client for HF Space.
-r server/requirements.txt
# Ensure Gradio client is installed (needed for Hugging Face toxicity API)
gradio-client>=0.15.0
